# Inside config.yaml
path: ./       # Current directory (the data folder)
train: images/train  # Relative path to training images
val: images/val     # Relative path to validation images
test: # (Optional) If you have test images, specify their path similarly

# Classes
names:
  0: car